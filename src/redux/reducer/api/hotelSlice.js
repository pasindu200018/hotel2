import { apiSlice } from "./apiSlice";

const USERS_URL = "/auth";

export const hotelApiSlice = apiSlice.injectEndpoints({
  endpoints: (builder) => ({}),
});

// export const { } = hotelApiSlice;    
